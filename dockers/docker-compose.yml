version: '3'
services:
  nginx:
    container_name: nginx
    image: nginx
    ports:
        - "80:80" 
        - "443:443" 
    volumes:
      - ../public:/usr/share/nginx/html/
      - ./etc/nginx/conf.d:/etc/nginx/conf.d
    tty: true
    
